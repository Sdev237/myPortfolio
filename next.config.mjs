/** @type {import('next').NextConfig} */
import { join } from 'path';

const nextConfig = {
  // pageExtensions: ['ts', 'tsx', 'js', 'jsx'],
  // webpack: (config) => {
  //   config.resolve.alias['@banque'] = join(__dirname, 'src/Projet/banque');
  //   return config;
  // },
};

export default nextConfig;
